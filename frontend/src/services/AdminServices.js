
/*
import { initializeApp, auth as adminAuth } from 'firebase-admin/app';

// Inicializar la aplicación de administración de Firebase
initializeApp();

// Obtener una instancia de Auth
const auth = adminAuth();


async function deleteUserFromAuth(uid) {
  try {
    await auth.deleteUser(uid);
    console.log('Usuario eliminado correctamente');
  } catch (error) {
    console.error('Error al eliminar usuario:', error);
    throw error;
  }
}

export default {
  deleteUserFromAuth
};
*/